# #1389 런타임 QA 보고서

- **실행 시각:** 2026-08-26 00:21 KST
- **PR:** #1533
- **QA 대상 runtime SHA:** `912c69e57323f6369fbef4853e93be79d2eacc8b`
- **Base:** `32ab0c7f65e62e6e69a799a2a5a5cd80ad71bb07`
- **환경:** local development Vite build 및 preview
- **범위:** DEV-only 재키 A/B/C 비교 화면, 48px 자산, friend-book-tree DEV cleanup migration, #1309 production/development 경계 보존

## 결론

자동·로컬 브라우저 QA와 stable DEV 배포 검증은 **PASS**다. A/B/C 중 canonical 후보는 선택하지 않았다. 사람의 실제 48px 시각 검수만 남아 있다.

## 발견 및 수정

### P1 — DEV cleanup migration workflow 불일치

- **원인:** `59_friend_book_tree_retirement.dev.sql`이 자체 `BEGIN/COMMIT`을 포함했으나 `migrate-dev.yml`은 workflow가 transaction을 소유한다.
- **수정:** migration 자체 transaction 문을 제거했다.
- **검증:** SQL parse PASS, transaction guard PASS, focused regression PASS.

### P1 — 비교 overlay의 modal 접근성 경계 부재

- **원인:** plain `section`으로 렌더되어 dialog announcement, initial focus, focus containment, Escape close, focus restoration이 없었다.
- **수정:** `role="dialog"`, `aria-modal="true"`, label/description, initial focus, Tab/Shift+Tab trap, Escape close, trigger focus restoration을 추가했다.
- **검증:** 정적 regression과 실제 Chromium keyboard QA 모두 PASS.

## 자동 검증

- `node --test tests/mascot-runtime.test.mjs` — PASS
- `pglast` parse of `59_friend_book_tree_retirement.dev.sql` — PASS
- migration 자체 `BEGIN/COMMIT` 부재 검사 — PASS
- development Vite build — PASS
- `EXPECT_MASCOT_REVIEW=1 node --test tests/mascot-runtime.test.mjs` — PASS
- `git diff --check` — PASS

기존 rebase gate에서도 다음을 확인했다.

- production build + `EXPECT_MASCOT_REVIEW=0` — PASS
- development build + `EXPECT_MASCOT_REVIEW=1` — PASS
- production/development personalization boundary — PASS
- spec-align — PASS
- design lint — PASS

Vite build에서 기존 font runtime-resolution 및 chunk-size warning이 출력됐지만 build는 성공했다. 이번 변경에서 새 build error는 없었다.

## 실제 브라우저 QA

local development build를 Chromium에서 열고 합성 검수 페르소나로 진입했다.

| 항목 | 기대 결과 | 실제 결과 |
|---|---|---|
| 비교 화면 열기 | modal dialog 생성 | PASS — `role=dialog`, `aria-modal=true` |
| initial focus | modal 내부 닫기 버튼 | PASS — active element `재키 후보 비교 닫기` |
| Shift+Tab | 첫 요소에서 마지막 요소로 wrap | PASS — 후보 C 마지막 `FAIL` 버튼 |
| Tab | 마지막 요소에서 첫 요소로 wrap | PASS — 닫기 버튼 |
| Escape | modal 닫기 | PASS — dialog 제거 |
| focus restoration | 원래 trigger로 복귀 | PASS — `재키 A/B/C 비교` |

초기 브라우저 시도는 기존 4174 서버를 잘못 가리켜 검증 대상에서 제외했다. 새 build가 실제 실행된 4175 preview에서 위 항목을 다시 수행해 PASS를 확인했다. 검증용 preview process는 종료했다.

## post-merge stable DEV 검증

- **검증 시각:** 2026-08-26 00:35 KST
- **PR merge commit:** `65c8b165f7d9d49120f9727ae861b7e7e30592ba`
- **deploy-dev run:** `32866462128` — PASS
- exact main SHA deploy — PASS
- stable DEV metadata·Supabase isolation — PASS
- stable DEV render smoke — PASS
- live development `/api/release` — `environment=development`, SHA `65c8b165f7d9d49120f9727ae861b7e7e30592ba`
- live Production `/api/release` — `environment=production`, SHA `d1c20b807bae55ed069b7691518ef602178c532a`; 기존 SHA 유지

## 경계 및 미완료 항목

- Production bundle에는 비교 module/UI/후보 자산을 포함하지 않는다.
- `.dev.sql`은 DEV migration workflow 전용이며 Production에 적용하지 않는다.
- Production은 변경하지 않았다.
- A/B/C canonical 후보 선택은 Hyu의 실제 48px 시각 검수 전까지 보류한다.
