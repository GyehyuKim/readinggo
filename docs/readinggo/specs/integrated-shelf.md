# 통합 서가 — 빈 서가 박멸 (#772 · #774)

> **신설 (2026-06-17)**: 오픈베타 헤비독서러 피드백에서 반복된 두 줄기를 하나로 묶은 spec.
> ① **능동** — 구매내역/서재 스샷 → 비전 OCR로 *내가 읽은 책* 복원 (#772, 태규).
> ② **수동** — 빈 책에 *남이 읽은 문장* 마중물 시드 (#774, 강민구). **실제 출처**(책속문장·네이버 블로그 API·서점 리뷰) — AI 생성 폐기(§5.2).
> 공통 목표: **빈 서가/외로움 박멸 + lock-in**(흩어진 독서를 한 서가에 모아 자산화).
> **편집 정책**: spec-only. 코드는 후속 PR 시리즈(MVP→확장). Stack Lock 준수(§5).

## 1. 목적·전략

- **문제**: 현재 최대 약점은 "초빈이라 심심하다"(빈 서가). 신규 유저가 들어와도 채울 게 없어 다음 행동으로 안 이어진다.
- **lock-in 가설**(태규): "책을 한 군데서 안 읽는다(알라딘·밀리·도서관…). 기록이 흩어져 안 보인다. 한 곳에 모아주면 정 붙고 그게 lock-in."
- **두 줄기**:
  - **① 능동 복원(#772)**: 유저가 스샷 한 장 올리면 → 비전 OCR → 내 서가 좌르륵. "스샷 한 장 = 독서 인생 복원".
  - **② 수동 마중물(#774)**: 데이터/감상이 빈 책 → 웹/AI에서 "남이 읽은 한 문장"을 시드로 깔아 와글한 느낌. 공감 → 나도 쓰고 싶어지는 루프의 출발점.
- **#772 = 내가 읽은 책(능동) / #774 = 남이 읽은 문장(수동)** — "빈 서가 박멸"의 양쪽 절반.

## 2. 영혼 보존 제약 (불가침 — 태규 #18)

통합/대량 등록으로 가는 순간 "또 하나의 독서 기록 앱"(북적북적류)이 되기 쉽다. 리딩고의 영혼은 **"한 문장 + AI 대화 + 참새"**라는 가볍고 따뜻한 레이어. **통합은 거들 뿐, 그 영혼을 끝까지 지킨다.**

- 대량 등록이 메인 UX가 되지 않게: 복원은 *온보딩/빈 상태의 1회성 마중물*로 위치. 평상시 핵심 동선은 여전히 한 문장·짹·참새.
- 복원된 책도 "한 문장 남기기"로 자연스럽게 유도(영혼 레이어 진입점).

## 3. 비목표

- **외부 서점/이북 API 자동 연동 금지**: 개인 구매이력 비공개·약관 리스크. → 스샷 업로드로 우회(연동 협의·약관 이슈 0).
- **대량 데이터가 제품의 중심이 되는 것**(§2).
- **타인 블로그 원문 무단 스크랩·전재**(§4.2 — 저작권 가드).

## 4. ① 스샷 OCR 서가 복원 (#772)

### 4.1 동선 (UX)

1. **진입(+로그인 게이트, #1048)**: ⓐ 내서재 빈 상태 CTA "📸 스샷으로 서가 복원" ⓑ 온보딩 선택 단계(옵션). 진입 시 미로그인(게스트)이면 모달 대신 **로그인 유도**(`RG_login()` + 토스트 "로그인하면 책을 가져올 수 있어요"). 로그인 사용자만 모달 진입(§4.7).
2. **업로드**: 알라딘/밀리/교보/왓챠 등 주문·서재·**표지 그리드** 캡쳐 1장(다중은 후속). 기존 `OcrCropOverlay` 자르기 재사용 가능(선택).
3. **처리중**: **비전(Gemini) 추출 1순위**(표지 그리드 인식) → 책 목록 추출(스피너 + "책 찾는 중…"). **큰/긴 스샷은 클라가 세로 조각으로 나눠 순차 처리** → "큰 사진이라 나눠 읽는 중 · N/M 조각" 진행 표시(#1045). 텍스트 리스트는 OCR 폴백.
4. **검수 리스트 — 모달은 "맞는 책인지 확인"만 (#1060)**: 인식된 책 카드 목록(체크박스 + 제목·저자 인라인 편집). 아닌 책은 체크 해제. 매칭 실패분은 "미확인" 표기. **별점이 인식되면 카드에 ★ 표시**(#1042, 추출 정보 표시). **책장(목적지) 토글은 모달에서 제거** — 그 결정은 검토함에서 항목별로 한다(모달↔검토함 '2번 검토' 중복 마찰 축소, #1060). 모달 카피가 "책장(읽고 싶은 책·읽는 중·읽은 책)은 다음 검토함에서 골라요"로 다음 단계를 안내.
5. **검토함 적재 + 목적지 결정(#1048·#1060)**: 선택분을 책장 직행이 아니라 **검토함(import_staging)** 에 담는다(제안 status=`completed`). "검토함에 N권 담았어요 — 책장에서 검토하세요" 토스트. 책장(서재)의 "가져온 책 · 검토" 뷰에서 **책장(읽고 싶은 책/읽는 중/읽은 책)을 항목별로 고르고** 이동/제외(§4.7) — 임포트의 *유일한* 목적지 결정 지점(모달은 확인만).

### 4.2 서버 — `POST /api/shelf-import` (비전 1순위 + OCR 폴백)

> **갱신 (#1042, 2026-06-27)**: 추출을 **비전(Gemini Flash) 1순위 + 텍스트 OCR(Upstage) 폴백**으로 교체. 실측 근거: 왓챠피디아 '평가한 작품들'(표지 그리드) 스샷 → 텍스트 OCR **0권**(표지 아트·잘린 제목·UI 텍스트가 뒤죽박죽), 같은 이미지를 **비전으로 보니 ~23/26권** 제목·저자·**별점**까지. 한국 유저 서재 스샷은 대부분 표지 그리드 → **비전이 핵심**. 텍스트 리스트(구매내역)는 기존 OCR 폴백으로 유지. (#1040 매칭·알라딘 보강·게스트 패리티·목적지 토글 위에 **추출 단계만** 교체.)

- 입력: `formData{ document: 이미지 }`. 8MB 제한(기존 `OCR_MAX_BYTES`).
- **① 비전 추출 (1순위, #1042)** — `GEMINI_API_KEY`+`VISION_*` 설정 시. `/api/extract-highlights`(#844)의 `callVision`(OpenAI 호환 chat completions + `image_url` base64, 지역 flap 재시도) **재사용** — system/userText 인자로 서가 프롬프트 주입.
  - 시스템 프롬프트(`SHELF_VISION_SYSTEM`, temperature 0.1): "책장/평가 목록 스샷에서 **보이는 책을 모두** 추출(표지로 알아본 제목·저자 포함). **별점(★/숫자) 보이면 `rating`(0.5~5.0) 함께**. UI·메뉴·광고·푸터·통계 숫자 제외. **확신 없으면 제외(지어내지 말 것)**. JSON 배열 `[{title, author, rating?}]`만."
  - 파싱: #1040의 관용 파서(`parseShelfBooks`) 재사용/확장 — 코드펜스·trailing comma 폴백 + **`rating` 0.5단위 스냅·0.5~5.0 클램프**(텍스트 OCR 경로엔 rating 미출현 → 무해).
  - 큰/긴 스샷(왓챠 풀페이지): 워커는 조각당 8MB 가드만(이미지 라이브러리 미도입 — Stack Lock), `max_tokens` 여유(2000)로 다수 책 안전. **큰 이미지 분할은 클라(canvas)에서 — §4.2.1**(실측: 풀이미지 한 장 호출은 Gemini가 큰 그리드의 일부만 읽어 1권, 클라 타일링 35권. 워커 한 장에 의존하지 않는다).
  - 비전이 책을 찾으면 그 결과 반환: `{ books, source:'vision' }`(rating 포함). **0건·호출 실패면 ② 텍스트 OCR 폴백**(가능할 때), 그것도 없으면 `{ books:[], empty:true }`.
- **② 텍스트 OCR 폴백 (구매내역 등)** — Upstage `model=ocr` → raw 텍스트 → `callLLM`(solar-pro3, temperature 0.1)로 책 목록 JSON 추출(`SHELF_EXTRACT_SYSTEM`). 출력: `{ books, raw, partial }`.
- 출력 공통: `{ books:[{ title, author, rating? }], source?, raw?, partial?, empty? }`. 키는 **서버 보관(클라 노출 금지)**.
- 실패/미설정: 비전·OCR **둘 다 미설정**이면 `503 { demo:true }`(기존 패턴) — 데모는 수동 추가 폴백.

### 4.2.1 클라(canvas) 타일링 — 큰 이미지 분할 (#1045)

> **결정 (#1045, 2026-06-27)**: 추출 **앞단만** 교체(매칭·검수·등록 #1040/#1038 흐름 동일). 긴 서재 스샷을 한 장으로 호출하면 Gemini 가 큰 표지 그리드의 일부만 읽어 회수율이 급락 — 실측 풀이미지 **1권** vs 클라 타일링 **35권**. 그래서 클라(canvas)가 큰 이미지를 세로로 분할해 조각마다 같은 `/api/shelf-import` 를 호출한다(워커·키·프롬프트 변경 없이 호출 **횟수만** N). 라이브 검증 완료.

- **크기 판단**: 이미지 디코드 후 `height > 2200px` **또는** `세로/가로 ≥ 2.3`(여러 화면 이어붙인 캡쳐)이면 타일링, 아니면(모바일 한 화면) **원본 그대로 단일 호출** — 현행 동작·화질 보존(재인코딩 없음).
- **분할(canvas)**: 가로 폭 유지, 세로 **~1800px 조각**, **~250px 겹침**(경계서 잘린 책을 다음 조각이 통째로 잡게 — 필수). 각 조각 `canvas.toBlob(image/jpeg, 0.85)`. 조각 수 안전 상한 12.
- **순차 호출 + 지연**: 조각을 **순차**로 보내고 **조각 간 ~1.8s 지연**(Gemini 무료티어 rate limit 대응 — #844 배치 OCR 순차+지연 패턴 차용). 진행 콜백으로 검수 전 "N/M 조각" 표시.
- **병합 + dedup**: 모든 조각의 `books` 를 합쳐 **정규화 제목(NFC·소문자·공백·구두점 제거) 완전일치로 dedup**(겹침서 같은 책 2번 제거 — #1052: 타일별 NFC/NFD·구두점 차이로 안 합쳐지던 "피로사회 ×3" 류 복구). `author`·`rating` 은 채워진 쪽 우선 보강.
- **UI 잡음 필터**: 추출에 가끔 버튼/탭 텍스트("오늘부터 독서시작" 등)가 섞임 — 소량 블랙리스트(정규화 완전일치)로 거른다(부분일치는 실제 제목 오삭제 위험이라 회피). 못 거른 잡음은 검수에서 체크 해제(무중단). 워커 `SHELF_VISION_SYSTEM` 도 UI 제외를 1차로 지시.
- **폴백**: 어느 조각이라도 `demo`(워커 미설정)면 즉시 중단해 데모 안내. 일부 조각 호출 실패는 부분 병합으로 무중단. 이미지 디코드 실패(희귀)면 원본 그대로 단일 호출.
- 오케스트레이션은 `RG_shelfImport.extractBooks(file, { onProgress })`(분할 `sliceToBlobs`·병합 `dedupeBooks` 포함) — UI 와 분리해 재사용/검증 가능.
- **후속(이번 범위 밖, #1045 B)**: 조각 추출의 **스테이징/스트리밍 검수**(조각이 찾는 즉시 카드 점증 노출)는 별도 PR.

### 4.3 매칭·미존재 처리

- 각 추출 책 → 카탈로그(`loadBooks`) **랭크 매칭**(#1038): `fuzzySearch` 후보를 제목 정확도로 점수화(완전일치>prefix>짧은 길이차) + 저자 일치 가산 → 최적 1건. 보수 가드(제목 양방향 정규화 포함)는 유지해 환각 표지·갖다붙이기 방지. 무순위 `hits[0]` 채택(구 동작)을 대체해 오매칭↓.
- **매칭 성공** → 카탈로그 `Book`(표지·메타 확보). 카탈로그 book(data.js 형태 `cover`/`total`/`isbn`)은 **어댑터 표면**(`cover_url`/`total_pages`/`isbn13`)으로 **정규화**해 등록 — 게스트(local)·로그인(Supabase) 모두 표지·쪽수·ISBN 보존(패리티, #1038 P1-2). 검수 카드 표지도 `cover_url`로 표시(P1-3).
- **매칭 실패** → **알라딘 보강**(#1038 P1-1, §4.3 구 약속 구현): 검수 진입 시 미매칭 책을 `/aladin` 검색(worker `ItemSearch`)으로 자동 보강(표지·쪽수·ISBN). 자동이 못 찾으면 검수 카드의 **"🔍 찾기" 버튼**으로 재시도(제목 편집 후). 알라딘 결과도 제목 정확도로 랭크. 워커가 결과를 Supabase `books`에 비파괴 upsert(#489)하므로 카탈로그도 점진 보강. 끝까지 미매칭이면 표지 없는 최소 책으로 등록 허용("미확인").
- 핵심 매핑·매칭·보강 로직은 `window.RG_shelfImport`(`normalizeCatalogBook`·`rankMatch`·`aladinLookup`·`matchRows`)로 추출 — 재사용 가능(#1039 유연 임포트 등). 비전 추출 `rating`(#1042)도 `matchRows` 가 행에 그대로 보존(매칭/미확인 무관)해 검수·등록까지 전달.

> **갱신 (#1052, 매칭 샤프닝)**: 실데이터 QA(빌린책 17권)에서 ① 같은 책이 타일별 NFC/NFD·구두점 차이로 여러 권 남고(예: "피로사회 ×3"), ② 보강(autoEnrich)이 백그라운드라 끝나기 전 검토함 적재 시 "미확인"으로 굳는 문제 확인(알라딘 프록시는 정상 — prod `/aladin` 대상 책 전부 ISBN 반환). 수정: **(a)** `_norm`을 **NFC 정규화 + 구두점 제거**로 강화(dedup·매칭 견고), **(b)** 검수 "검토함에 담기"를 **보강 완료까지 비활성**(미확인 조기 적재 방지), **(c)** 보강 후 **isbn13 dedup**(`_dedupRowsByIsbn` — 제목 표기 무관 동일 책 합침), **(d)** **enrich-on-commit** — `importStaging.commit`이 미확인(book_id·isbn 없음) 행을 책장 삽입 직전 `aladinLookup`으로 ISBN·표지·쪽수 해소(기존 검토함 미확인 행도 [내 서재로] 이동 시 채워짐 — "DB엔 ISBN" 보장).

### 4.4 일괄 등록 (DataStore 계약 확장)

> **갱신 (#1048)**: 검수 "등록"은 이제 책장에 직행하지 않고 **검토함(import_staging)** 에 적재한다(§4.7). 아래 `myBooks.addBatch` 배치 등록은 **검토함 → 책장 이동(`importStaging.commit`)** 단계에서 재사용된다(status 라우팅·별점 보존 로직 그대로). 즉 추출→매칭→검수 산출물의 *최종 목적지*만 "책장"에서 "검토함"으로 한 칸 당겨졌고, 등록 계약 자체는 보존.

- 검수 후 선택분을 **배치 등록**: `myBooks.addBatch(items[]) → UserBook[]`(양 어댑터 구현). `items[i] = { book, status, rating? }`.
- **목적지 토글**(#1038 도입 → **#1060 위치 이동**): 책장 선택은 **읽은 책(`completed`) / 읽고싶어요(`wish`) / 읽는 중(`reading`)** 셋 중 하나. 구(#1038)는 검수 *모달*의 일괄 토글이었으나, **#1060에서 모달↔검토함 '2번 검토' 중복을 줄이려 토글을 모달에서 제거하고 검토함으로 옮겼다**(§4.7) — 모달은 제안값 `completed`만 보내고, 사용자는 검토함에서 **책별로** 책장을 고른다(일괄보다 세밀). 모달은 "맞는 책인지 확인"에 집중.
- **status 라우팅**(`addBatch`): `wish` → `wish_books`(위시 UX 일치 — Supabase 는 `books.upsert`로 캐노니컬 id 확보 후, local 은 메타를 `BOOK_BY_ID`에 시드해 getBook 해소). `completed`/`reading` → `user_books`(`add` 재사용, 중복 자동 스킵).
- **별점 보존**(#1042): 비전 추출 `rating` → `addBatch` item 으로 흘러 `user_books.rating` 에 보존(양 어댑터). 0.5~5.0 0.5단위 스냅(Supabase `ub_rating_range` CHECK 준수). `wish` 경로는 `wish_books` 에 별점 컬럼이 없어 **무시**(읽고싶어요엔 별점 의미 없음). 기존 user_book 재사용 시엔 **별점이 비어있을 때만** 채움(기존 평가 비파괴).
- 게스트(미로그인)는 local 어댑터로 보관 후 로그인 시 흡수(§7.7 backend.md).

### 4.5 폴백·실패 UX

- 인식 0건 → "스샷에서 책을 못 찾았어요. 더 선명한 캡쳐로 다시 시도하거나 직접 추가하세요." + 수동 추가 진입.
- 부분 인식(`partial`) → 인식분만 보여주고 "일부만 찾았어요" 안내.

### 4.6 프라이버시·분석

- 업로드 스샷은 **처리 후 미저장**(워커 메모리 경유, 영속 저장 없음). 원본은 분석·리플레이에 미포함.
- 이벤트: `shelf_import_started` / `shelf_import_extracted{count}` / `shelf_import_staged{count, status}`(#1048 검수→검토함 적재) / `shelf_import_registered{count, status}`(#1048 검토함→책장 이동) (analytics.md §3 필수=익명 집계). `status`=목적지 토글값(#1038).

### 4.7 로그인 게이트 + 검토함 스테이징 (#1048)

> **결정 (#1048, 2026-06-27)**: 임포트를 **로그인 사용자 전용**으로 막고, 검수 "등록"을 책장 직행 대신 **검토함(import_staging)** 으로 보낸다. 이유 두 가지 — ① **비전 LLM 코스트**(게스트 무제한 호출 차단) ② **임포트는 실계정에 영속**(localStorage 게스트는 로그인 시 흡수 경로가 따로 필요해 일관성↓). 추출·매칭·검수(§4.2~4.4)는 그대로, **앞단(게이트)·뒷단(검토함)** 만 추가.
>
> **갱신 (#1060, 2026-06-29)**: 베타 피드백 — "스샷 복원에서 검토 레이어가 2번(모달 검수 + 서재 검토함)이라 1번 느낌으로 줄였으면". 검토함(영속 큐·로그인 게이트·비전 코스트)은 **유지**하고, **모달을 슬림화**해 중복 마찰만 줄인다: 모달 검수에서 **책장(목적지) 토글을 제거**해 모달은 "맞는 책인지 확인"(선택·추출 검증)만 하고, 무거운 **목적지·이동 결정은 검토함의 책별 토글로 단일화**한다. '검수 2번'이 '확인(모달) → 결정(검토함)'으로 역할 분리돼 중복감이 사라진다.

#### A. 로그인 게이트

- `RG_openShelfImport`(app.js) 진입 시 **로그인 여부 확인**: Supabase 설정 + 미로그인(게스트, `authUser === null`)이면 **모달 대신** `RG_login()` 호출 + 토스트 "로그인하면 책을 가져올 수 있어요". 로그인 사용자만 `ShelfImportModal` 진입.
- 서재(library.js)의 두 진입 버튼("📸 스샷으로 복원" 상시·빈 서가 CTA)은 모두 `RG_openShelfImport` 경유 → 게이트 자동 적용(중복 구현 없음).
- 순수 로컬 데모(`!_supa`)는 게이트 비적용(모달 진입)이나 워커 미설정이라 추출이 `demo`로 떨어져 등록 단계 도달 안 함(무해).

#### B. 검토함 스테이징 (로그인 전용)

- **테이블** `public.import_staging`(`37_import_staging.sql`, **수동 1회 적용**) — backend.md §7.3. 사용자별 임포트 검토 큐. RLS 본인만(`user_id = auth.uid()`). 영속(세션 넘어 유지).
- **어댑터** `DataStore.importStaging`(backend.md §7.2) — 양 어댑터 시그니처 일치:
  - `add(items)` — 검수분([{book, status, rating?}], `addBatch` 표면)을 행으로 평탄화해 적재(매칭/보강 메타 보존, book_id=canonical 매칭 시).
  - `list()` — 본인 검토함 목록(최신순).
  - `remove(id)` — 제외(영구 삭제).
  - `commit(id, status)` — **책장 이동**: status(없으면 `suggested_status`)로 `myBooks.addBatch` 라우팅(§4.4 등록 로직·별점 보존 재사용) → 성공 시 staging row 삭제.
  - **local 어댑터(datastore.js)는 no-op**(게스트는 게이트로 도달 안 함) — 계약 표면 패리티만 유지(같은 시그니처, 빈 동작).
- **임포트 흐름 변경**(shelf-import.js): 검수 "등록"이 `myBooks.addBatch` 직행 대신 `importStaging.add`로 적재 + "검토함에 N권 담았어요" 안내(`rg:import-staged` 이벤트로 서재 검토함 갱신). **모달은 "맞는 책인지 확인"만**(#1060) — 책장(목적지) 토글 제거, 제안값 `completed`만 보낸다.
- **검토함 뷰**(library.js, 내 서재 섹션 상단): 검토함이 비지 않으면 **"📦 가져온 책 N권 · 검토"** 패널 노출 →
  - 항목별: 표지·제목·저자·**매칭상태(매칭됨/확인됨/미확인)**·별점 + **책장 토글(읽은책/읽고싶어요/읽는중**, 기본 `suggested_status`=`completed`) + **[내 서재로 이동]**(`commit`, 별점 보존) **[제외]**(`remove`). 이 **책별 책장 토글이 임포트의 유일한 목적지 결정**이다(#1060 — 모달 토글 제거 후 여기로 단일화).
  - **매칭상태 3단계 (#1066)**: `book_id` 있음 → **매칭됨**(카탈로그 매칭) / `isbn13` 있음(book_id 없음) → **확인됨**(알라딘 보강으로 ISBN·표지 확보, #1052) / 둘 다 없음 → **미확인**. 긍정 2단계(매칭됨·확인됨)는 브랜드색·굵게, 미확인만 흐리게(`--ink-3`). 이유: 보강으로 ISBN·표지가 채워진 책을 "미확인"으로 표기하면 데이터가 빈 것처럼 오해를 준다(실제로는 enrich-on-commit으로 정상 등록).
  - 일괄: **[전체 이동]**(각 항목 현재 토글값으로 commit) / **[전체 제외]**.
- **별점 보존**: 비전 추출 `rating`이 검토함 행(`import_staging.rating`)에 보존되고 `commit` → `addBatch` → `user_books.rating`까지 흐른다(`wish` 경로는 §4.4대로 무시).

## 5. ② 웹 리뷰 마중물 시드 (#774)

### 5.1 가치·원칙

- 핵심 가치는 *메타데이터 채우기가 아니라* → **외롭지 않게·와글한 느낌·마중물**. 남의 기록에 공감받고("당신도 그랬군요") 나도 쓰고 싶어지는 루프의 출발점.
- **진짜 유저 콘텐츠와 명확히 구분**(투명성).

### 5.2 채택 — 실제 출처 시드 (AI 생성 폐기)

**결정 (#774, 2026-06-17)**: "AI가 감상을 지어낸다"(구 A안)는 **폐기.** LLM은 책을 실제로 안 읽었으므로 만들어낸 문장은 진짜 인용이 아니고 감상도 두루뭉술한 *가짜* → 계휴가 후회한 "없는 거 뻥으로 만들어놨던" 문제 재현. **진짜 출처에서 가져온다.** 정식 라이선스 계약은 학생·데모 단계에서 불가능 → **크롤/공개 API가 유일한 현실 경로**(계휴 결정: 초기 고객경험 위해 불가피, 비영리).

**컨셉 교정 (2026-06-18, 계휴)**: 1차 구현이 LLM으로 블로그 글을 **요약·재작성**해 "독자가 안 쓴 문장"을 만들어 둔 게 발견됨(감사 65건 중 진짜 책 인용/독자 글 5건 미만, 출판사 소개·평론·광고 다수). 검토 끝에 컨셉을 못박음: **원하는 건 블로거의 감상이 아니라, 그 글 안에서 독자가 옮긴 "책 원문 인용"**(예: 블로그 인용 블록 속 책 속 문장). → 소스를 **네이버 블로그 본문의 인용 블록(`blockquote`·스마트에디터 `se-quote`)**으로 잡고, LLM은 인용 후보 중 책 원문만 **선별·필터**(생성 금지), **발췌 검증**으로 강제. **감상 추출**(글쓴이 생각 혼입)·**알라딘 리뷰**(API가 리뷰 본문·목록 미제공, 집계 `ratingInfo`만 옴)는 **폐기**. 기존 시드 전량 재생성.

> **저작권 입장(솔직)**: 저작권은 비영리에도 원칙 적용된다. 다만 ① 비상업·교육/데모 ② **출처 표기** ③ **인용 범위 최소(한 문장~짧은 스니펫, 전문 전재 금지)** ④ robots/약관 best-effort ⑤ **권리자 요청 시 즉시 삭제** 가드를 두면 실무 리스크는 작다. **정식 상용화 전 재검토**를 본 spec에 못박는다. *준공식 API(네이버 블로그 검색) + 출처 제공* 경로를 1순위로 써 리스크를 더 낮춘다.

### 5.3 소스 (다중 — 우선순위)

| 우선 | 소스 | 내용 | 리스크 | 비고 |
|---|---|---|---|---|
| 1 | **책 속 문장**(예스24 "책속의 한 문장"·알라딘 "책속에서"·교보 "책속으로") | 진짜 *그 책* 문장 | 낮음(출판사 홍보용 발췌) | "한 문장" 컨셉에 가장 적합 |
| 2 | **네이버 블로그 검색 API** | 서평·독후감 스니펫 + 출처 링크 | 낮음(준공식·출처 자동) | 대량·안전, 키는 secret |
| 3 | 서점 독자 리뷰(예스24 한줄평·알라딘 100자평·교보 클로버) | 진짜 독자 감상 | 중(약관·인용범위) | 출처·스니펫 제한 |
| — | 약함: goodreads(영어)·밀리/리디(로그인벽)·네이버 책 API(2020 종료) | — | — | 제외 |

- 1·2를 MVP, 3은 후속. 모든 시드는 **출처(sourceName·sourceUrl) 보존**.

### 5.4 서버 — `POST /api/seed` (영속·목표 10개, #774 · TTL #806 갱신 2026-06-18)

- **규칙 (계휴 결정)**: 책 한 권의 **문장(실 유저 + 시드)이 10개 미만**이면 시드를 채워 **10개까지** 만든다. 한 번 채우면 **영속**(다음부터 즉시·콜드스타트에도 유지) → "내가 픽한 책엔 글이 계속 있다".
- **워커 중심 영속**: `/api/seed`가 **Supabase `seed_sentences`(§5.7)에 직접 read/write**(service role). 클라는 `/api/seed`만 호출(직접 DB 안 건드림).
- 입력: `{ title, author, isbn, have }` — `have`=클라가 이미 가진 실 공개 문장 수(`byBook` 길이). `need = max(0, 10 - have - 저장된시드수)`.
- 동작:
  1. `book_key`(= `isbn13` 있으면 그것, 없으면 정규화 제목) 로 `seed_sentences` 조회.
  2. `have + 저장시드 ≥ 10` → 저장분 그대로 반환(재크롤 안 함). **일반 요청(`/api/seed`)은 영구 고정** — TTL 재크롤은 cron만(아래 §TTL).
  3. 부족하면 `need`개만큼 수집 → **블로그 글 속 '책 원문 인용'을 그대로 발췌**(LLM은 인용 선별·노이즈 필터만 — **재작성·요약·생성 금지**, #774 컨셉 교정 2026-06-18) → **새 시드 `seed_sentences` insert**(중복 url 스킵) → 누적 반환.
     - **소스·추출**: 네이버 블로그 검색(`책속 문장`/`밑줄`/`인상깊은 구절` — 인용 담긴 글 유도) → **모바일 본문(`m.blog.naver.com`) fetch → 인용 블록(`blockquote`·`se-quote`)에서 책 인용 후보 추출**(`seedBlogQuotes`). 책 인용은 글쓴이 감상과 달리 인용 블록으로 표시되는 경우가 많아 **'글쓴이 생각' 혼입을 줄인다.**
     - **LLM 필터(`SEED_EXTRACT_SYSTEM`)**: 후보 중 **책 본문 문장만** 남기고 소제목·목차 라벨·블로거 감상·책 소개·`명언/명대사` 꼬리표·출처표기 제거. 모호하면(글쓴이 생각인지 책 원문인지 불확실) 버림. 쓸 만한 인용이 없으면 **0건**(가짜로 채우지 않음 — LLM 미작동 시에도 raw 블록 저장 금지).
     - **발췌 검증**: LLM 출력이 후보 원문에 실제로 존재하는지 확인(공백 제거 후 앞부분 포함). 재작성/창작이면 버림.
     - **폐기**: 감상 추출(글쓴이 생각 혼입)·알라딘 리뷰(`reviewList` API가 본문·목록 미제공). 구 `읽고/독후감/서평` 검색어도 폐기.
- 출력: `{ seeds: [{ text, sourceName, sourceUrl }] }` (저장분 + 신규, 최대 10−have).
- 키(네이버·service role)는 **서버 secret**, 클라 노출 금지. 실패/0건 → 빈 배열(무중단).
- 비용: 책당 1회 충전 후 영속 → 반복 호출은 DB read만(LLM·네이버 재호출 없음).
- **전 책 백필(계휴 결정)**: 책 수가 적고 lazy 첫 호출이 느리므로, `docs/readinggo/supabase/seed_backfill.mjs`로 **카탈로그 전 책을 1회 선충전**(books 순회 → `/api/seed have=0` → seed_sentences 채움, 제한 동시성). idempotent(이미 채워진 책은 재호출 비용 0). **인기순(`sales_point` desc)으로 순회** — 베스트/스테디셀러부터.
- **인기 우선 cron 선충전(#774)**: 워커 `scheduled()`(일 1회)에 `prewarmSeeds(env)` — `books`를 `sales_point` desc 상위 N권(기본 150) `seedFill`. idempotent라 사실상 **신규·미충전 인기 책만** 일함 → 수동 백필 없이도 자주 찾는 책이 미리 채워짐. 쿼터·비용 안전.
- **시드 TTL — 트렌드 갱신(#806, cron 전용)**: 베스트셀러 리뷰는 시간에 따라 바뀐다. cron이 인기책을 돌 때 **가장 최신 시드(`seed_sentences.created_at`)가 `SEED_TTL_DAYS`(기본 30일) 경과**면 만료로 보고 **재크롤 → 기존 시드 삭제 후 새 시드로 교체**(`seedRefresh`). 재크롤이 0건이면 **기존 유지**(빈 화면 방지). **일일 재크롤 상한 `SEED_REFRESH_DAILY_CAP`(기본 20)** 로 만료 책이 한꺼번에 몰려도 쿼터·비용을 분산. **일반 `/api/seed` 요청에는 미적용**(영구 고정 유지) — TTL 갱신은 cron만 수행.
- **쿼리 폴백**: `제목 저자 서평 → 서평 → 리뷰 → 독후감 → 책` 순으로 넓혀 0건율↓(네이버 AND 매칭 보완). 동명이작은 LLM 프롬프트로 제외.
- ⚠️ **커버리지 한계**: 시드는 네이버가 실제 반환하는 양에 달림. 검증 환경에선 일부 인기 책도 0건 — 실제 네이버 도달/키 제한 여부는 네이버 콘솔 API 테스트로 확정 필요(추정 금지).

### 5.5 표시

- 책 상세에서 실 공개 문장(`byBook`)이 **10개 미만**이면 `/api/seed`로 부족분을 채워 "🌱 함께 읽은 이웃" 섹션에 노출(실 문장과 합쳐 ~10). 실 문장 ≥10이면 시드 미호출(자연 소멸).
- 실제 유저 문장과 **시각 구분**(🌱 배지·톤 다운) + **출처 링크 표기**(저작권·진정성). 짹/대화 허용 범위는 §7(권장: 읽기 전용 — 짹 무의미).
- *주의*: 구현 #796은 "0건일 때만" 노출(live fetch). 본 갱신(영속·10개 충전)은 마이그레이션 적용 후 후속 코드 PR에서 `0건 → <10` 으로 확장.

### 5.6 진정성 — "뻥"으로 안 보이게

- **🌱 마중물 라벨 + 출처**: 시드임을 투명 표기, *진짜 유저인 척 위장 금지*(신뢰 붕괴 방지). 출처가 있어 오히려 진정성↑.
- **자연 소멸**: 진짜 유저 문장이 쌓이면 시드는 **후순위→숨김**. 마중물은 *초기 공백만* 채운다.

### 5.7 데이터 모델 — 별도 `seed_sentences` 테이블 (#774 갱신)

`sentences`는 `user_id`·`user_book_id` **NOT NULL** + `byBook`이 user_books **inner join** 이라 "유저 없는 시드"를 담기 부적합. 데모 책 id도 비-UUID('b008'). → **별도 테이블**, **책 키 = isbn13/제목**(Phase 0/1·데모 공통), **워커 service role 전용**(클라 직접 접근 없음 → 공개 RLS 불필요).

```sql
create table public.seed_sentences (
  id          uuid primary key default gen_random_uuid(),
  book_key    text not null,          -- isbn13(우선) | 정규화 제목
  text        text not null,          -- 한 문장(정제), 인용 최소
  source_name text,                   -- 블로그/매체 이름
  source_url  text,                   -- 원글 링크(저작권·진정성)
  created_at  timestamptz not null default now()
);
create index on public.seed_sentences(book_key);
-- 중복 방지: 같은 책+같은 출처 url 1회만
create unique index on public.seed_sentences(book_key, source_url);
alter table public.seed_sentences enable row level security;
-- 정책 없음 → anon/authenticated 차단, service_role(워커)만 bypass. 권리자 삭제는 운영 콘솔/SQL.
```

- 마이그레이션: `docs/readinggo/supabase/30_seed_sentences.sql` (**수동 적용** — memory[[supabase-migrations-manual]]).
- 클라는 `seed_sentences`를 직접 읽지 않고 `/api/seed` 응답만 표시 → Phase 0/1 어댑터 무관.

### 5.8 리스크 매트릭스 (실제 출처)

| 리스크 | 가드 |
|---|---|
| 저작권 | 출처표기·인용범위 최소·robots·삭제대응·비영리·상용화 전 재검토 / 1순위는 책속문장·네이버 API(준공식) |
| 진정성("뻥") | 🌱 라벨 + 출처 링크 + 자연 소멸 |
| 품질·안정성 | LLM 정제·중복제거·캐시·실패 시 섹션 숨김(무중단) |

## 6. Stack Lock 정합 (CLAUDE.md)

- **OCR**: v8 결정(2026-06-11) — 웹 OCR 허용, Upstage Document OCR + solar-pro3, **키 서버 보관**. #772는 이 라인 **확장**(새 프레임워크 0).
- **크롤/생성**: 서버리스 워커(`worker/index.mjs`)에 엔드포인트 추가, 키 서버 보관. 클라 노출 금지.
- **DataStore 계약**: 피처 코드는 저장소 직접 호출 금지. `myBooks.addBatch`·시드 read는 계약 경유(backend.md §7.2 확장).
- **빌드**: React 18 CDN + Babel 유지. 신규 모듈은 #761 패턴(`shelf-import.js` 등 별도 파일 + `window.X`).

## 7. 결정·미해결

- **[결정] #774 방향**: **실제 출처 시드**(책속문장 + 네이버 블로그 API → 후속 서점 리뷰). AI 생성 폐기. 비영리·출처표기·인용 최소 가드(§5.2).
- **[결정·구현 #1038] #772 등록 status**: 검수 단계 **목적지 토글**(읽은 책=`completed` 기본 / 읽고싶어요=`wish` / 읽는 중=`reading`) 일괄 1개 → 그 status 로 `addBatch`. 책별 status 는 후속.
- **[결정·구현 #1048] 로그인 게이트 + 검토함 스테이징**: 임포트는 **로그인 전용**(게스트 차단 — 비전 코스트·실계정 영속), 검수 "등록"은 **검토함(import_staging)** 적재 후 서재 "검토함" 뷰에서 항목별/일괄 이동·제외(§4.7). 별점 보존. local 어댑터는 no-op(계약 패리티만).
- **[결정·구현 #1060] 검토 레이어 슬림화 + 찾아 담기 승격**(베타 피드백): ① 검토함은 유지하되 **모달의 책장 토글을 제거**해 모달=확인·검토함=목적지 결정으로 역할 분리(§4.4·§4.7 — '2번 검토' 중복 마찰 축소). ② **'책 찾아 담기'를 '내 서재' 타이틀 아래로 승격**(library.js) — 구는 '읽고 싶은 책' 탭 하위라 위시 전용처럼 보였다. 상시 노출 + 카피로 읽고 싶은 책·읽는 중·읽은 책 다중 책장을 드러냄.
- **[미해결] 시드 상호작용**: 짹/대화 허용 범위 — **권장: 읽기 전용, 대화는 검토**.
- **[미해결] 다중 스샷**: MVP는 1장, 다중은 후속(#1038에서 미구현 — 코어 정리·알라딘 보강 우선).

## 8. Phasing (MVP cut)

| Phase | 범위 | 리스크 |
|---|---|---|
| **P1 (MVP)** | #772 스샷 OCR 서가 복원(1장·검수·일괄 등록) | 낮음(OCR 스택 재사용) |
| **P2** | #774 실제 출처 시드 — 책속문장 + 네이버 블로그 API, 🌱 라벨·출처·자연 소멸 | 중(저작권 가드로 관리) |
| **P3** | #774 서점 독자 리뷰 확장(예스24·알라딘·교보) | 중 — 출처·인용범위 강화 |

- P1·P2 독립(능동/수동) → 각각 코드 PR. 공유 파일(index.html·align) 충돌 회피 위해 직렬 머지. **#772(P1)부터 착수**(핵심·리스크 최저).

## 9. 검증 체크리스트 (코드 PR마다)

- [ ] 서버 키 클라 미노출(Stack Lock) · 8MB 제한 · 실패 폴백 무중단
- [ ] 큰/긴 스샷 클라 타일링(세로 분할·~250px 겹침·순차+지연·정규화 dedup·UI 잡음 필터), 작은 이미지 단일 호출 폴백 유지 (#1045)
- [ ] DataStore 계약 경유(직접 저장소 호출 0) · `importStaging` 양 어댑터 시그니처 일치(supabase 실구현 / local no-op) (#1048)
- [ ] 로그인 게이트(게스트 차단 → RG_login+토스트) · 검수 "등록"=검토함 적재 · 서재 검토함 뷰 이동(commit, 별점 보존)/제외(remove) (#1048)
- [ ] 시드 = 🌱 마중물 라벨 + 출처(sourceName·sourceUrl) 표기, 인용 범위 최소, 진짜 유저 문장 우선
- [ ] 영혼 보존(§2): 복원/시드 후 "한 문장" 진입점 유지
- [ ] boot-smoke · spec-align · 프리뷰(비프로모션) 통과
